/**
 * Module created to work around the
 * {@link https://bugs.chromium.org/p/chromium/issues/detail?id=1111297 Chrome's bug} that prevents
 * opening the source code of the worker in that browser.
 *
 * @module worker-gate
 * @see module:worker
 */

import './worker';
